# Cline Rules — GBT Pro v2.1

## 项目身份
- 名称: GBT Pro v2.1
- 类型: AI 驱动 A 股自主交易 Windows 桌面终端
- 语言: Python 3.12
- 入口: desktop/app.py (Flask + pywebview)
- 端口: 8877

## 硬性规则 (Hard Rules)

### 编码
1. **所有 `subprocess.run()` 必须带 `errors='replace'`** — 中文 GBK 环境
2. **Python `write` 工具操作文件，禁止 PowerShell `Set-Content`** — 破坏 UTF-8
3. **禁止在 dataclass 中用 `open` 作字段名** — Python 保留字
4. **`Position` 是类不是 dict** — 用属性访问，不用 `.get()`
5. **`.env` 必须是 `KEY=value` 格式** — 不能放 shell 脚本

### 桌面操控
6. **WebView2 缓存绕过** — 换端口是唯一可靠方法，HTTP headers 无效
7. **JS 语法验证** — `new Function(js)` 校验后再提交，WebView2 静默丢坏 script
8. **pyautogui FAILSAFE=True** — 永远开启，鼠标移到角落即中止
9. **桌面操控必须真实执行** — 禁止空壳能力、假数据、模拟汇报

### 能力注册
10. **新能力必须注册到 `gbt/capabilities.py`** — 统一路由器管理
11. **能力处理器必须调用真实 API/系统调用** — 返回真实数据
12. **修改能力后运行 `tests/test_router.py`** — 验证无回归

### 构建
13. **每次编辑后 `py_compile` 验证语法** — Python 3.12
14. **打包前确认依赖在 Python312 下安装** — PATH 可能指向 3.11
15. **打包用 `python build_exe.py`** — 输出 dist/GBT/GBT.exe

## 架构地图
```
桌面窗口 (pywebview) ← Flask (8877) → LLM (DeepSeek v4-pro)
    ↓                      ↓
 layout.html          ┌────┴────┬──────────┬──────────┬──────────┐
 styles.css          brain    trader    watcher   desktop_ctl router
                     (大脑)   (交易)    (守夜人)   (操控)     (调度)
                        ↓        ↓         ↓          ↓          ↓
                    tech_analysis strategies risk_ctrl  account capabilities
```

## 禁止事项
- ❌ 伪造行情数据 / 模拟交易结果
- ❌ 空壳能力处理器（只返回文字不执行操作）
- ❌ 硬编码关键词路由（用 router 注册表）
- ❌ 跳过风控直接执行交易
- ❌ PowerShell 操作 UTF-8 文件
- ❌ 在非交易时段假装有实时行情
- ❌ 假数据填充（数据为空就报空，不编造）

## 测试命令
```bash
# 语法检查
python -c "import py_compile; py_compile.compile('gbt/brain.py', doraise=True)"

# 路由器全能力测试
python tests/test_router.py

# API 健康检查
curl http://localhost:8877/api/status

# 打包
python build_exe.py
```
